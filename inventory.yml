all:
  hosts:
    ubuntu_machine:
      ansible_host: 'learn.linux'
  vars:
    ansible_user: 'ubuntu'
    ansible_become: true
    ansible_become_method: 'sudo'
    ansible_python_interpreter: "/usr/bin/python3"
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'